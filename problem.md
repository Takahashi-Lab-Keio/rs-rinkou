• 適切なのは「RGB-Dを用いた人体関節の3次元位置・速度推定」です。対象関節はまず右膝の1点に限定し、発展課題として鼻・右肩・右腰・右足首にも適用さ
  せる構成がよいと思います。

  重要なのは、MMPoseと深度が同じ量を別々に測っているわけではない点です。

  - MMPose：画像上の座標 (u, v) と信頼度
  - RealSense：その画素に対応する奥行き d
  - カメラ内部パラメータ：(u, v, d) を3次元座標 (X, Y, Z) に変換
  - カルマンフィルタ：観測された3次元位置と運動モデルを時間方向に融合

  したがって、「深度と姿勢をカルマンフィルタへ直接2種類の観測として入れる」というより、「RGBと深度から得た3次元関節位置を、運動モデルによって安
  定化・補間する」問題になります。

  ## 推奨する課題

  ### RGB-D人体関節追跡におけるカルマンフィルタの設計

  RealSenseとMMPoseを用いて右膝の3次元位置を計測する。カルマンフィルタを設計し、ノイズ、欠測、急な運動が存在する状況でも、関節位置と速度をリアル
  タイムに推定せよ。

  学生へ与える入力は次のようにします。

  時刻 t
  画像上の関節座標 u, v
  MMPose信頼度 c
  関節周辺の深度値
  カメラ内部パラメータ

  学生には以下を考えさせます。

  1. 状態ベクトルに何を含めるか
  2. どのような運動モデルを仮定するか
  3. 観測方程式をどう定義するか
  4. プロセスノイズ共分散 Q と観測ノイズ共分散 R をどう決めるか
  5. 信頼度低下や深度欠測をどう扱うか

  ## 状態方程式を考えさせる部分

  最初から状態ベクトルを指定せず、次の要求だけを提示するのがよいです。

  > 関節の3次元位置だけでなく、短時間先の位置を予測できるモデルを設計せよ。

  標準的な解答は、等速度モデルです。

  x = [X, Y, Z, Vx, Vy, Vz]ᵀ

  学生にはサンプリング時間 Δt を含む状態遷移行列を導出させます。例えばX方向なら、

  X(k+1) = X(k) + Δt Vx(k)
  Vx(k+1) = Vx(k)

  となります。これを3軸へ拡張させます。

  比較対象として、次のモデルを検討させてもよいです。

  - 位置だけを状態にするモデル
  - 位置＋速度の等速度モデル
  - 位置＋速度＋加速度の等加速度モデル

  モデルを複雑にすれば必ず改善するわけではなく、状態数、応答性、ノイズ、初期値依存性の違いを考察させられます。

  ## 観測方程式

  深度をRGB画像へ位置合わせした後、MMPoseの座標 (u, v) に対応する深度 d を取得します。それをRealSenseの内部パラメータで逆投影し、

  z = [Xmeas, Ymeas, Zmeas]ᵀ

  という3次元位置観測を作ります。

  等速度モデルの場合、観測行列は状態から位置成分だけを取り出す形になります。ただし、行列自体は学生に導出させるのが課題の狙いに合います。

  3年生向けには逆投影処理をスターターコードとして与え、4年生向けには逆投影も実装課題にできます。

  ## 重み調整の実験

  QとRを適当に決めるだけで終わらせず、実験データから調整させるのが重要です。

  ### 実験1：静止

  被験者が10秒間静止します。

  ここでは本来関節座標が一定なので、観測値の分散を測定できます。この結果からRの初期値を決めさせます。

  評価項目：

  - 生データの標準偏差
  - フィルタ後の標準偏差
  - X、Y、Z方向でノイズ量が異なるか
  - 深度方向のノイズが大きいか

  ### 実験2：ゆっくりした屈伸

  被験者がゆっくり膝を曲げ伸ばしします。

  Qを小さくしすぎると滑らかになりますが、実際の運動に対して推定が遅れます。Qを大きくすると追従性は上がりますが、観測ノイズも通りやすくなります。

  少なくとも以下を比較させます。

  Qが小さい場合
  基準値
  Qが大きい場合

  評価項目：

  - 推定値の滑らかさ
  - 動作開始時の遅れ
  - 動作停止後の収束
  - 推定速度が妥当か

  ### 実験3：急な動作

  右膝を素早く動かして停止します。

  等速度モデルと等加速度モデル、または異なるQを比較します。ここでは「モデルが実際の運動に合わないとき、プロセスノイズが何を補っているか」を考察
  させます。

  ### 実験4：一時的な遮蔽

  手や物体で右膝を一時的に隠します。

  以下の処理を実装させます。

  - MMPose信頼度が閾値未満なら観測更新を行わない
  - 深度が0なら観測更新を行わない
  - 観測がない間は予測だけで継続する
  - 観測復帰後に推定がどう戻るか確認する

  ## 実践的な発展課題

  固定したRだけでなく、MMPoseの信頼度を使ってRを変化させます。

  例えば考え方として、

  信頼度が高い → 観測を信頼する → Rを小さくする
  信頼度が低い → 観測を疑う → Rを大きくする

  とします。

  深度についても、関節周囲の小領域から中央値とばらつきを計算できます。

  深度のばらつきが小さい → Rzを小さくする
  深度のばらつきが大きい → Rzを大きくする
  深度が無効 → 観測更新を行わない

  具体的な変換式は与えず、例えば次の形の係数を学生に調整させます。

  Rxy = 基準値 / (信頼度 + ε)
  Rz  = 基準値 + 深度周辺の分散 × 係数

  これは理論だけでなく、現実のセンサに合わせて重みを調整する経験になります。

  ## 提出物

  提出物は以下が適切です。

  1. 状態ベクトルとその選定理由
  2. 状態方程式と観測方程式
  3. Q、R、初期共分散Pの設定方法
  4. 生の観測値と推定値を重ねた時系列グラフ
  5. 3種類以上のQ、Rを比較した結果
  6. 静止、屈伸、遮蔽実験の結果
  7. 滑らかさと追従性のトレードオフについての考察
  8. 実装コード

  ## 評価基準の例

   項目                        配点
  ━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━
   状態・観測モデルの妥当性      25
  ──────────────────────────  ──────
   QとRの決定方法                20
  ──────────────────────────  ──────
   リアルタイム実装              15
  ──────────────────────────  ──────
   静止・運動・遮蔽実験          20
  ──────────────────────────  ──────
   グラフと定量評価              10
  ──────────────────────────  ──────
   考察                          10

  単に「見た目が滑らかになった」だけではなく、静止時の標準偏差と運動時の遅れを数値で比較させるのがポイントです。

  学部3年生向けなら右膝1点・等速度モデル・固定Q/Rまでを必須にし、学部4年生向けなら信頼度に応じたRの変更、欠測処理、複数関節への拡張まで求める
  と、難易度を調整しやすいです。
